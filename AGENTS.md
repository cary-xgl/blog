# Agent Notes

## 提交规范

- 默认在完成代码、内容结构或配置改动，并通过必要验证后，将改动提交至 git。
- 提交信息遵循 `docs/git-commit-convention.md` 中的规范。
- 提交前先检查 `git status`，避免把无关改动混入当前提交。
- 当前 Hugo 博客项目在本地开发时，提交前验证默认使用 `hugo` 或 `hugo --minify`，不使用 `--cleanDestinationDir`，避免把正在运行的 `hugo server` 状态冲掉。

## 主题定制

- 主题定制优先使用项目本地覆盖方式处理，例如根目录下的 `layouts/`、`static/`、`assets/`；尽量不要直接修改 `themes/` 下的子模块代码。
- 只有在本地覆盖无法实现，或明确决定长期维护主题分支时，才修改子模块代码。

## 文字优化

- 只要用户说“优化表述”“去机器味”“收一收”，默认同时应用 `文字优化`、`技术文章偏好`、`技术文章优化顺序` 这三块，不单独挑一条用。
- 默认按自然笔记风格去收文章：保留原意和口吻，先让句子更清楚、更紧凑，再去掉报告腔、讲解腔和 AI 式总结。
- 不为了显得口语而硬加口头禅，也不为了显得正式把句子抻长。
- 保留原有知识点、链接、图片引用和结构意图；可以调整分段、标题、表格和列表，让后续阅读和维护更轻松。
- 技术文章优先保留工程记录感，不强行改成教材或说明书。
- 正文标题默认从 `##` 开始，不用 `#`，也不要直接从 `###` 起。
- 开头 `>` 里的简介默认写得轻一点，更像随手留下的一句题外备注，不要写成摘要、导读或标准答案，也不要把正文结论提前讲完。

## 图片展示

- 文章内如果需要手动控制图片尺寸，优先使用原生 `img` 标签。
- `content/photography/碎片化.md` 这类摄影碎片记录页，图片默认使用 `style="width: auto; max-height: 500px;"`，除非当前页面有明确的特殊展示需求。

## 文章总结配图

- 当用户需要先总结文章、再生成配图时，默认可以按文章结构分段总结，再组织成适合出图的内容，不强行压成一张信息过满的大图。
- 这类图片默认使用固定模板风格：
  - `sketchnote` 白板视觉笔记风格
  - 米白背景，马克笔线稿，轻微水彩

## 技术文章偏好

- 开头直接进问题，少铺垫。
- 句子尽量短一点，连接尽量轻一点，不一层层解释“为什么这里要写这段”。
- 接受术语，但不要写成培训材料那种标准口吻。
- 先说这个东西有没有用、会不会生效、哪里容易失效，再补背景。
- 可以带一点自己的判断，但不要端着教人，也不要故意做出口语感。
- 少用太满的收束句、拔高句和结论腔，像 `核心在于`、`本质上`、`通常更适合` 这类词少用。
- 比起讲完整，更在意像自己写的笔记，所以允许留白，不必每一节都补成完整教程。
- 判断尽量收进内容本身，少用“别上来就”“不是因为...而是因为...”这类讲解感很强的句式。
- 小标题直接一点，中性一点，不刻意追求观点感。
- “常见误区”“好处”“考虑点”这类内容，可以直接列，不用再加很多导语。
- 技术点解释主动收口，不顺手把一句常识讲成一整段。
- 主线已经清楚时，不再补一层“怎么区分”“更直接的理解”这类重复解释。
- 用户只点到一小块内容时，就只补那一小块，不顺手扩成更大的专题。
- 需要画流程图时优先用 Mermaid.js，其次再考虑简约 SVG 或静态图片。
- `>` 简介不要写成“本文将从……讲到……”“这篇系统梳理了……”，更像自己开头留的一句备注。
- 少用“这几个点容易混”“放在一起看”“区别如下”这类整理腔词，尤其不要把它们直接放进 `>` 简介和小标题里。
- 少写“先记一下”“总结”“下面说”“这一篇主要讲”这类写作动作本身，尤其在 `>` 简介和小标题里，内容直接站住就行。

## 技术文章优化顺序

- 先保留原文结构和信息密度。
- 再删掉模板化解释、总结腔和说明书腔。
- 最后只做必要的顺句，不主动扩背景，不替作者补标准答案。

## 压缩记忆

- 压缩记忆时，文件级文字优化细节优先剔除，只保留稳定偏好、约定和未完成事项。
