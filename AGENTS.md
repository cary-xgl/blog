# Agent Notes

## 提交规范

- 默认在完成代码、内容结构或配置改动，并通过必要验证后，将改动提交至 git。
- 提交信息遵循 `docs/git-commit-convention.md` 中的规范。
- 提交前先检查 `git status`，避免把无关改动混入当前提交。
- 当前 Hugo 博客项目在本地开发时，提交前验证默认使用 `hugo` 或 `hugo --minify`，不使用 `--cleanDestinationDir`，避免把正在运行的 `hugo server` 状态冲掉。

## 主题定制

- 主题定制优先使用项目本地覆盖方式处理，例如根目录下的 `layouts/`、`static/`、`assets/`；尽量不要直接修改 `themes/` 下的子模块代码。
- 只有在本地覆盖无法实现，或明确决定长期维护主题分支时，才修改子模块代码。

## 文字优化

- 中文文章优化默认采用当前博客的自然笔记风格：保留原意和作者口吻，减少报告腔、讲解腔和 AI 式总结；优先让句子更清楚、更紧凑，不为了显得口语而硬加口头禅。
- 优化文章时保留原有知识点、链接、图片引用和文章结构意图；可以调整分段、标题、表格和列表，让后续阅读与维护更轻松。
- 针对技术文章的润色，优先保留“工程记录感”而不是“完整讲解感”：允许保留一点判断、经验感和不完美，不强行改成教材或说明书。
- 优化文章时，正文标题层级默认从 `##` 开始，不用 `#` 作为正文标题，也不要从 `###` 或其他层级直接起。

## 图片展示

- 文章内如果需要手动控制图片尺寸，优先使用原生 `img` 标签。
- `content/photography/碎片化.md` 这类摄影碎片记录页，图片默认使用 `style="width: auto; max-height: 500px;"`，除非当前页面有明确的特殊展示需求。

## 技术文章偏好

- 开头直接进问题，不喜欢铺垫式引入。
- 更偏短句和轻量连接，不喜欢一层层解释“为什么这段存在”。
- 接受术语，但不喜欢被翻成过度标准化的培训口吻。
- 喜欢“先说这个东西有没有用、会不会生效、哪里容易失效”，再补背景。
- 喜欢保留一点口语化判断，例如“这一点比较容易漏”“这块更别扭一点”，但不喜欢夸张口癖。
- 不喜欢过强的总结句、收束句、拔高句，也不喜欢 `核心在于`、`本质上`、`通常更适合` 这类高频 AI 连接词反复出现。
- 比起“讲完整”，更在意“像自己写的笔记”，所以允许局部留白，不必每一节都补成完整教程。
- 比起强调作者态度，更偏好把判断收进内容本身，少用“别上来就”“不是因为...而是因为...”这类带明显讲解感的句式。
- 小标题和段落命名更偏中性、直接，像 `核心参数`、`线程池参数配置` 这种就够了，不刻意追求有观点的标题。
- 喜欢把“常见误区”“好处”“考虑点”这类信息直接列出来，不需要额外加很多导语去铺垫。
- 对技术点的解释会主动收口，宁可少一点修辞和类比，也不要把一句常识写得太满。
- 不喜欢为了“讲完整”再补一层重复解释；主线已经清楚时，不再额外加“怎么区分”“更直接的理解”这类重复收束段落。
- 对“补充一点内容”的请求，默认按小力度处理，只补用户点到的核心差异，不顺手扩成更大的专题。
- 不默认补流程图；尤其不要用 markdown 代码块充当流程图。如果确实需要配图，优先用真正简约的图片或 SVG，而不是伪流程图。

## 技术文章优化顺序

- 先保留原文结构和信息密度。
- 再删掉模板化解释、总结腔和说明书腔。
- 最后只做必要的顺句，不主动扩写背景知识。

## 压缩记忆

- 压缩记忆时，文件级文字优化细节优先剔除，只保留稳定偏好、约定和未完成事项。
