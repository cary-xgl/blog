---
title: "印象笔记待确认"
date: 2026-05-31
tags: ["待确认", "Evernote"]
categories: ["技术"]
---

> 这些内容已经从导出文件里分离出来了，但暂时不直接并入正式文章。
<!--more-->

## 暂时放这里的原因

- 有些内容更像内部项目记录，不适合直接按通用技术笔记发布。
- 有些内容本身太碎，强行整理成文章反而会失真。
- 有些内容和现有文章明显重复，后面更适合按主题合并。
- 有一小部分带个人或公司信息，不适合直接提交到仓库。

## 内部项目与业务记录

- `1049`：项目介绍、接口优化、联通、货拉拉、Eureka
- `1001`：团队现状、验证框架对比、Redis key 前缀建议
- `1011`：Disruptor、MySQL 锁、组件二次开发
- `1019`：HAP、FreeIPA、JWT、OAuth、Nacos、网关
- `989`：外部交互设计、Insight 代码随笔、线上问题排查、日志设计

## 碎片化基础笔记，后面更适合按主题并入

- `501`：NIO/BIO、ThreadLocal、Spring Security、SSO
- `575`：Java 基础、Spring、SpringBoot、Spring Session、Log4j、Spring Security、Hibernate
- `658`：Redis、MySQL、Java、Spring、消息队列、项目、自问
- `654`：原码、补码、反码

## 明显重复或已并入现有文章的内容

- `595`：Git 命令，和现有 `git命令.md` 高度重复，这次不再单独导入。
- `531`：正向代理、反向代理和 Nginx location，后面更适合并到网络或网关相关笔记里。
- `553`：表名、列名和配置表规则，更像项目内部规范，暂时不单独成文。

## 敏感内容

- `613`：包含个人/公司信息，这部分不进入正式文章，也不参与提交。
